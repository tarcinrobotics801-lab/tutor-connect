services:
  
  backend:
    build: ./back-end
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    restart: always  
      
  frontend:
    build:
      context: ./front-end
    ports:
      - "7000:80"
    depends_on:
      - backend
    restart: always  

 

