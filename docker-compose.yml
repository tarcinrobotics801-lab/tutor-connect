services:
  
  backend:
    build: ./back-end
    ports:
      - "5002:5000"
    environment:
      - PORT=5000
    restart: always  
      
  frontend:
    build:
      context: ./front-end
    ports:
      - "7002:80"
    depends_on:
      - backend
    restart: always  

 

